.DS_Store

# Full-resolution source clips (40-50 MB each). The deployed background is the
# compressed Freight/freight-bg.mp4 — keep the masters out of the repo and out
# of the published site.
Freight/freightBackground*.mp4
